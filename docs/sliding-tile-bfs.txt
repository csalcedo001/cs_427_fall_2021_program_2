The BFS algorithm explores each layer of depth from the starting position, but the search space is huge, so it is not expected that it will turn out to find any solution unless it is within a small distance from the starting position.
